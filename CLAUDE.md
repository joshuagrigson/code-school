# Notes for Claude

- Always spell it **color** (colors, colored, recolor), never "colour" — in UI
  text, lesson copy, code comments, commit messages and the README. The only
  "colour" left in `code-lab.html` is on purpose: lines that teach the British
  spelling as the mistake, and answer lists that accept either spelling.
- The class id `warden` is shown to people as **Guard**; the id stays `warden`
  so existing saves keep working.
- `code-lab.html` must keep exactly one `</script>`, no `<!--`, and exactly
  one `</body>`.
